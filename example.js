# here's a comment
# multi-line

rule ZipCodes() {
  1 + 2 * 3;
  something();
  withArgs(3, "a", token);
  -3 + 4 * 5;
#  conform length(5);
#  conform type("string");
#  conform allowedChars(Numeric);
}

rule Email() {
}
